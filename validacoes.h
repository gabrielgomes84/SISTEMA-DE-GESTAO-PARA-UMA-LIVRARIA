#ifndef VALIDACOES_H
#define VALIDACOES_H

int validarLetras(const char *str);
int validarNumeros(const char *str);
int validarPreco(const char *preco);
int validarLivro(const Livro livro);
int validarCPF(const char *cpf);

#endif
